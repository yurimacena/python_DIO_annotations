# [].append
# [].clear = limpar listas
# [].copy = copia lista, mas com instâncias diferentes
# [].count = conta quantas vezes um determinado objeto aparece na lista
# [].extend = mescla duas listas
# [].index = mostra o índice de determinado valor
# [].pop = cada comando pop exibe cada elemento, de trás pra frente(a não ser que você passe um índice)
# [].reverse = espelha uma lista ao contrário
# [].sort = ordena alfabetifabente a lista
    #(reverse=True) = inverte lista
    #(key=lambda x: len(x)) = ordena por tamanho das palavras
    #(key=lambda x:len(x)) = essa ordenação por tamanho se inverte
#len() = mostra o total de elementos da lista
#sorted() = ordena interaveís, a diferença é que sorted é uma FUNÇÃO